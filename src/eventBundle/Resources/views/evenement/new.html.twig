{% extends 'base2.html.twig' %}

{% block body %}
    <h1>Création d'un événement</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Ajouter l'événement" onclick="verif()" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('evenement_indexadmin') }}">Revenir à la liste</a>
        </li>
    </ul>
{% endblock %}
