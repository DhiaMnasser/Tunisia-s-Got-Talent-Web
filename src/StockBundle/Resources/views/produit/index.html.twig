{% extends 'base.html.twig' %}

{% block body %}
    <h1>Produits list</h1>

    <table>
        <thead>
            <tr>
{#                <th>Id</th>#}
{#                <th>photo</th>#}
                <th>Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for produit in produits %}
            <tr>
{#                <td><a href="{{ path('produit_show', { 'id': produit.id }) }}">{{ produit.id }}</a></td>#}
{#                TODO: ajouter image   #}
{#                <td> <img src="{{ asset('')}}"> </td>#}
                <td>{{ produit.nom }}  </td>
                <td>
                    <button><a href="{{ path('produit_show', { 'id': produit.id }) }}">show</a></button>
                    <button><a href="{{ path('produit_edit', { 'id': produit.id }) }}">edit</a></button>
                    <a href="{{ path('lignecommande_new', { 'id': produit.id }) }}">buy</a>
                </td>

            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('produit_new') }}">Create a new produit</a>
        </li>
    </ul>
{% endblock %}
