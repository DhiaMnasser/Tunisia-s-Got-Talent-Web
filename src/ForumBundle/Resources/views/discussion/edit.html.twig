

{% block body %}
    <h1>Discussion edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('post_index') }}">cancel</a>
        </li>

    </ul>
{% endblock %}
