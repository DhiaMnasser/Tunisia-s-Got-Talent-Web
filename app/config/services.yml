# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html
parameters:
    #parameter_name: value
    upload_directory: '%kernel.project_dir%app/uploads/videos/publication'

services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    eventBundle.service.listener:
        class: eventBundle\EventListener\CalendarEventListener
        arguments: [ "@doctrine.orm.entity_manager" ]
        tags:
            - { name: 'kernel.event_listener', event: 'calendar.load_events', method: loadEvents }

    # makes classes in src/AppBundle available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    AppBundle\:
        resource: '../../src/AppBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/AppBundle/{Entity,Repository,Tests}'

    # controllers are imported separately to make sure they're pub
    # and have a tag that allows actions to type-hint services
    AppBundle\Controller\:
        resource: '../../src/AppBundle/Controller'
        public: true
        tags: ['controller.service_arguments']

    # add more services, or override services that need manual wiring
    # AppBundle\Service\ExampleService:
    #     arguments:
    #         $someArgument: 'some_value'

    upload.annotation_reader:
        class: Gth\UploadBundle\Annotation\UploadAnnotationReader
        arguments: ["@annotations.reader"]
        public: true

    upload.handler:
        class: Gth\UploadBundle\Handler\UploadHandlerGth

    upload.subscriber:
        class: Gth\UploadBundle\Listener\UploadSubscriber
        arguments: ["@upload.annotation_reader","@upload.handler"]
        tags:
            - { name: doctrine.event_subscriber,connection:default }

    user.form.registration:
        class: UserBundle\Form\RegistrationType
        tags:
            - { name: form.type, alias: user_user_registration }

    app.chart.data:
        class: AppBundle\Chart\ChartData
        arguments: ['@doctrine.orm.entity_manager']

        app.chart:
            class: AppBundle\Chart\Chart
            arguments: ['@app.chart.data']

    panier_services:
        class: AchatBundle\Controller\PanierController
        arguments: [ '@doctrine.orm.entity_manager' ]



